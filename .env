# Store download links — leave blank to show "Coming soon" instead of a fake link
VITE_ANDROID_DOWNLOAD_URL=
VITE_IOS_DOWNLOAD_URL=

# Base URL of the real backend API (leave blank to run the admin dashboard
# against local mock/demo data instead)
VITE_API_BASE_URL=
